VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
VITE_INGESTION_WORKER_URL=https://your-worker-name.your-subdomain.workers.dev

# These same two values must also be set as GitHub repository secrets
# (Settings -> Secrets and variables -> Actions) so the Pages deploy
# workflow can bake them into the production build.
